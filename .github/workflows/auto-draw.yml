name: Lottery Auto Draw
on:
  schedule:
    - cron: '*/5 * * * *'  # every 5 minutes
  workflow_dispatch:

jobs:
  autodraw:
    runs-on: ubuntu-latest
    steps:
      - name: Auto Draw Overdue Lotteries
        run: curl -s "https://www.kismathub.com/api/cron/lottery-autodraw"
